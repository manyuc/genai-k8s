apiVersion: v1
kind: Service
metadata:
  name: genai-api
  namespace: genai-rollouts
  labels:
    app.kubernetes.io/name: genai-rollouts-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: genai-rollouts-api
